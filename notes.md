# NOTES

<br>

### Compile Executable

---

1)  Comment `stockfish` requirement in `requirements.txt`
2)  Remove `stockfish` directory from `data`
3)  Add `"SF"` to `E.BOT_EXCLUDE_ALL` in `Constants.py`
4)  Run `pyinstaller main.spec` in directory cmd
